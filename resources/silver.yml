resources:
  jobs:
    silver_pipeline:
      name: "[portfolio] Silver Pipeline"
      tasks:
        - task_key: run_validated_silver
          notebook_task:
            notebook_path: ${workspace.file_path}/pipelines/notebooks/run_validated_silver
            base_parameters:
              catalog: ${var.catalog_name}
          timeout_seconds: 7200
          max_retries: 1
          retry_on_timeout: true
